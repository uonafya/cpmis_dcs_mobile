import 'package:flutter/material.dart';

class MedicalWidget extends StatelessWidget {
  const MedicalWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Medical Widget');
  }
}