import 'package:flutter/material.dart';

class CaseDataWidget extends StatelessWidget {
  const CaseDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Case Data Widget');
  }
}
