import 'package:flutter/material.dart';

class AboutChildWidget extends StatelessWidget {
  const AboutChildWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('About Child Widget');
  }
}
